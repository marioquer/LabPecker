<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.marioquer.labpecker">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_labpecker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_labpecker"
        android:supportsRtl="true"
        android:theme="@style/Night">
        <activity android:name=".UI.TeacherMainActivity" />
        <activity
            android:name=".UI.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.CourseActivity" />
        <activity android:name=".UI.StudentMainActivity" />
        <activity android:name=".UI.ProjectDetailActivity"></activity>
    </application>

</manifest>